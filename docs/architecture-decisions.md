# 产品架构决策记录（ADR）

> 起于 2026-08-29 · 记录"为什么这样设计"的方向与架构决策，避免反复推倒重来。
> 与 [PROJECT_STATUS.md](./PROJECT_STATUS.md) 的关系：本文档记**方向与接口决策**；PROJECT_STATUS 记**已实现的功能**。

## 一、产品定位（2026-08-29 讨论收敛）

**一句话：给学生的 AI 学习引擎 + 考研适配 —— 内核通用，考研做透，数据从大一攒到上岸。**

- **主战场**：考研人全旅程（长线准备 → 正式备考 → 冲刺 → 二战/在职）
- **内核通用**：计划/打卡/番茄/错题/RAG 资料问答与考试无关，天然通用；自由度高、不锁死考研
- **不做其它考试**：不服务考公/法考等（各考试结构差异大，贪多嚼不烂，稀释考研深度）
- **大一用户**：架构支持通用（降门槛），运营先聚焦考研（有强共性）；是否正式主推大一，**用数据决定**

## 二、核心差异化（回答"为什么不用通用 chatbot"）

- 通用 chatbot：用户要自己把它调教成考研专家，每次重喂上下文，AI 记不住你的计划/错题/进度
- **我们的 AI**：接进一套完整备考数据 + 工具（建任务/收录错题/查资料/出题/评测），**开箱即用**，AI 的每个动作直接写进备考体系
- 技能系统是它的进阶延伸：用户编排"AI 怎么用我的数据"；官方做深核心闭环，长尾需求交给社区

## 三、已定方向

### 3.1 自由度（拉高，不锁死考研）
- 不强制"考研目标"才能用；用户可手动自建计划，也可让 AI 生成非考研的科目计划
- 计划生成入口第一问：「你要为哪件事做计划」→ **计划类型分发**（考研备考 / 课程期末 / 自学单科 / 考证…）
- 这同时服务：方向未定的大三用户、课程学习的大一用户、自学者

### 3.2 测评（消抵触设计）
- **不叫考试**：叫"水平自检 / 现状扫描"；低风险明示（"答对答错不影响任何东西"）、短快即时价值、渐进出现
- **自选讲解三态**：答对且真会 → 可跳过讲解；答对但"蒙的" → 标记后进讲解 + 按"未掌握"处理；答错 → 默认展开讲解
- "蒙对"标记价值：① 提升测评数据质量（蒙对 ≠ 掌握，避免水平/择校判断偏乐观）② 元认知训练
- 测评产出 → 水平 → **择校匹配**（复用 admission 分数线数据）

### 3.3 技能插件化（从"对话工作流" → "数据+逻辑+表现的声明式小应用"）
- **三层**：数据层（白名单快照 + 技能档案 note + 输入）/ 逻辑层（AI 步骤 + 事件→动作）/ 表现层（组件）
- **AI 只是其中一种数据源和逻辑器，不是全部**（背单词不需要 AI 也能成立）
- **两种宿主**：向导流（一次性流程）/ 工具页（可反复使用的页面，如背单词页）
- **组件调色板**：题卡（从 practice 抽象）/ 闪卡 / 数据展示 / 进度 / 按钮 / 表单 + AI 输出渲染
- **能力分级**：官方组件 → 组合组件 → 受限表达式 → 沙盒代码组件（重工程，后置）
- **分享**：技能 = 结构化 JSON 工件，分享 = 详情页 + 一键安装；信任机制复用 admission 经验（多来源 + 点赞/质疑）
- **受限表达式**：能"算"不能"做"的迷你语言（纯函数、无副作用、不访问外部），V2/V3 的进阶能力

### 3.4 激活漏斗页（决策依据，先做）
- **定位**：admin 诊断页，只给开发者看，不是产品功能
- 阶段：注册/激活 → 设目标 → 生成计划 → 首次打卡 → 配 AI → 用过深功能 → 7 日回访
- 形态：阶段转化率 + 单用户轨迹（点开一个卡住的用户看他的使用痕迹）
- 目的：目前所有方向讨论都在猜，先让数据说话

### 3.5 引导（从"解释功能"到"让用户知道他在做什么"）
- 每个最小行动配方向感：**要做什么 / 为什么 / 做完得到什么 / 处在第几步**；跳过永远在
- 首访一句话引导（每页首次进入一条横幅）+ 术语去化（知识图谱→知识点关系图等）
- 阶段性评估：解释功能 < 让用户先做成一件小事

### 3.6 卡片 DIY = 技能工具页宿主（布局引擎共用）

**联动关系**：工作台卡片自由组合（排序/网格）与技能插件化「工具页」（如背单词页）**共用两层地基**：① 组件契约（D2：输入/状态/动作/渲染统一接口）② 布局引擎（位置 + 预设尺寸档位，对应 D1 的 render/layout 留位）。

- **两者区别**：工作台卡片 = 数据源是 dashboard 服务端查询、官方维护、固定生命周期；技能工具页 = 数据源是技能档案 note + 白名单快照、用户/社区编排、即插即用可分享
- **意义**：画布版 DIY 不是「用户摆卡片」的纯功能，而是**平台投资**（一笔投入，工作台 + 技能工具页两处受益）；顺序错了（先做工具页再做画布）＝布局引擎写两遍
- **设计原则**：① 编辑是显式模式（点「编辑布局」进入，仿 iOS 抖动态，平时无把手）② 尺寸只用预设档位（1/2/4 栏 × 1/2 行），不做任意像素缩放 ③ 出厂仍是精心设计的默认布局（新用户不是空画布）④ 移动端降级为排序列表
- **数据模型留口**：`workspaceCards` 保持有序数组起步；`CARD_REGISTRY` 加可选 `sizes: ['full','half','quarter']` 元数据字段；将来升级 `{id, size, pos}` 不破坏旧数据（zustand migrate 有先例）
- **分阶段**：阶段 0 只做默认布局收敛 + 数据模型留口；阶段 1 内测数据决定是否做排序版（1-2 天试水）；网格引擎随阶段 2 技能工具页宿主一起做

### 3.7 能力出口与本地分层（MCP/CLI/桌面壳，2026-08-29 晚定）

**MCP/CLI 定位**：它们是我们的**能力出口**（数据出口 + 自动化出口），不是用户功能。MCP 让"我们的 AI 被任何客户端调用"，CLI 让"我们的能力被脚本/桌面壳调用"。**不直接服务考研学生，服务"我们的数据能走向哪里"——这是数据资产护城河的技术表达。**

- **MCP 分工（修正 D6 偏自研方向）**：技能 = **MCP server（数据快照 + 动作，走开放标准）+ 表现声明（组件 + 布局，自研对齐 MCP Apps）**。数据/工具层接 MCP（白嫖生态、跨客户端兼容），表现层自研（布局引擎 + 组件系统，考研垂直手感），**不是全自研也不是全外包**。MCP Apps（2026-07 发布）已补上"UI 交互"能力，表现层形状朝它对齐。
- **MCP vs CLI 不站队**：主流共识是分层不是二选一。产品内部继续用 HTTP API + tool-calling（本来就够，用户网页里用不到 MCP/CLI）；对外用**薄适配层双出口**（同一份业务逻辑，包一层 MCP server + 一层 CLI），一两天成本。
- **Web vs 软件分层**：web 是正确地基（PWA 覆盖日常 95%），桌面壳（Tauri，复用现有代码）是**第二层超能力**，解锁本地 DB / MCP server / CLI / OS 小组件。现在不加壳，阶段 1-2 技能需要本地宿主时再加。
- **阶段 0 结论**：MCP/CLI/桌面壳都不做，专注把网页内核心闭环做稳 + 修狗粮日志坑；薄适配层等技能生态启动时一两天补上。

## 四、架构决策（必须现在定，防推倒重来）

| # | 决策 | 内容 | 不定会怎样 |
|---|------|------|-----------|
| **D1** | 技能 JSON schema **版本化** + 可扩展 step 类型 | 为 render/layout（页面组件步骤）留位，保持向后兼容 | 技能系统锁死在"对话流"，长不成"页面应用" |
| **D2** | **组件契约**接口文档 | 输入 / 状态 / 动作 / 渲染 定成接口，官方组件先实现 | 将来开放用户自制组件要推倒 |
| **D3** | Goal **自由度** | `university` / `examDate` 可空，或加轻量"方向"概念 | 上线后再加要数据迁移 |
| **D4** | **计划类型分发**接口 | generate-plan 不写死考研，抽象成"类型注册表 + 分发" | 写死后加通用计划要重构 |
| **D5** | **AI 内容护栏 + 技能能力白名单** | 合规红线前置：能力收紧（白名单数据 + 平台 AI 路由）/ 内容红线（作弊、代考、侵权、违法）/ 社区机制（举报、信任、审核） | 合规事故（尤其代考作弊）代价远超收益 |
| **D6** | **技能 = 接 MCP 数据/工具 + 自研表现层（双出口）** | 技能数据快照/动作走 MCP 开放标准（白嫖生态、跨客户端兼容），组件契约 + 布局引擎自研对齐 MCP Apps；对外包薄适配层双出口（MCP server + CLI） | 全自研协议 → 跟 Anthropic 的 MCP 撞车且必输；全外包 → 表现层无考研手感 |

**原则**：推倒重来的风险不在"功能没做够"，在"接口和 schema 定得不对"。功能是增量（加字段、加路由），接口错了才要重写。D1–D6 现在定，其余全部可后补。

## 五、七条线程三问评估（能不能做 / 有没有必要做 / 做到什么程度）

| 线程 | 能不能 | 必要性 | 程度 |
|------|--------|--------|------|
| 定位收敛 | 可行，内核已通用 | 必要（方向地基） | 现在决策期；落地只改文案/入口 |
| 激活漏斗页 | 很简单（admin 页 + 聚合查询） | 最必要（决策依据） | 只做阶段转化 + 单用户轨迹 |
| 引导改造 | 容易（前端 + ui-store 标记） | 必要 | 首访一句话先做；场景入口后做 |
| 多阶段/长周期 | 可行，最小版不碰 schema | 必要（考研全旅程核心） | 阶段推导 + 文案/计划跨度分档；完整建模看数据 |
| 自由度计划 | 可行（类型分发抽象） | 必要但**不急** | 意图第一问 + 3 种官方类型；社区贡献别现在做 |
| 测评体系 | 可行，复用 practice/模考 | 必要（"学习产品 vs 管理系统"分水岭） | 自选讲解 + 蒙对标记近期做；水平→择校等阶段化后 |
| 技能插件化 | 能，但分级；沙盒是重工程 | 定位招牌，但近期对用户未必必要（长期投资） | **架构决策现在定，实现做最小**（官方组件 + 工具页宿主） |

## 六、待决问题

- 技能宿主先做"工具页"还是"向导流"？（当前倾向工具页——留存型、可反复使用）
- V1 只做官方组件调色板，还是包含"组合组件"（用户把几个官方组件存成新组件）？
- 计划类型近期先做哪几种官方类型？
- 大一用户：只"降门槛"还是"正式主推"？（用激活漏斗数据决定）
- 技能分享链接（层②）作为"着重做"的落点，起步做导出/导入还是直接做详情页+安装？

## 七、分阶段路线（2026-08-29 补充）

**原则：小、可逆、为后续留接口。** 第一阶段先让开发者自己用起来——产品先服务一个人（开发者本人，2027 考研、大三），再用真实数据校准方向。

| 阶段 | 时期 | 目标 | 主要动作 | 退出/进入条件 |
|------|------|------|----------|----------------|
| **0 自证** | 现在～10 月中 | 让开发者自己用起来，产品产生第一批真实数据 | A 大三长周期阶段化（最小版）+ B 激活漏斗页 + C 狗粮测试 | 连续用 2 周 **或** 明确说出"为什么不用"（两个结果都算完成，都用来校准） |
| **1 校准** | 10 中～12 月 | 学院第一批用户内测，拿真实反馈 | 老师转发 + 同学组队内测（5-10 人）；定切口（择校/测评/计划三选一）；落地最便宜的已定方向（引导/术语/自由度） | 拿到 N 条真实反馈 + 漏斗数据 |
| **2 深化** | 2027 上半年 | 做深切口 | 测评体系（自选讲解+蒙对标记+水平→择校）；计划类型扩展（期末/自学）；技能插件化 V1（组件契约 D1/D2 + 工具页宿主 + 官方组件）——**布局引擎（工作台网格 DIY = 技能工具页宿主）随本轮一起做** | — |
| **3 扩展** | 后置 | 数据决定 | 技能分享/JSON 工件/技能库；大一用户主推；二战/在职覆盖 | 视阶段 0-2 数据 |

### 阶段 0 详案（本周 3-4 天，均不碰 schema、可回退）

> **进度（2026-08-29）**：✅ A / ✅ B / ✅ D6 种子 / 🔄 C 狗粮测试（持续记录 `docs/usage-notes.md`）。

- **✅ A 代码 · 大三长周期阶段化（最小版）**：`derivePrepStage(examDate, now)`（探索/基础/备考/冲刺四段，原"长线/正式/冲刺"三档已并入）→ 驱动倒计时文案（今日状态头"备考周期 · 当前：X期"）+ **计划跨度提示**（`STAGE_CONFIG.planSpanHint`，tasks 页阶段卡展示 + 注入 generate-plan prompt/响应）。**已以 `STAGE_CONFIG` 配置形式实现**（label/planSpanHint/planPhase，`stageToPlanPhase` 读配置）—— 为 D4 计划类型分发留接口（加计划类型=加配置）。
- **✅ B 数据 · 激活漏斗页**：`/admin`「用户激活漏斗」tab（📊 默认打开）—— 注册/激活→设目标→生成计划→首次打卡→配 AI→深功能→7 日回访 转化率 + 单用户轨迹（点开看各环节首触）。实现：`src/lib/funnel.ts`（纯逻辑）+ `/api/admin/funnel`（requireAdmin）+ `funnel-view.tsx`。
- **🔄 C 行为 · 狗粮测试**：开发者用产品设 2027 考研目标 → 生成本周任务 → 打卡；每次"用不下去"记入 `docs/usage-notes.md`，形成真实 backlog。

### 接口预留（D1-D6 现状）

- 阶段 0 只落 **D4 种子**：阶段推导写成配置，未来加计划类型=加配置
- **D1/D2**（技能 schema 版本化 + 组件契约）：本次不碰，技能 JSON 保持向后兼容
- **D3**（Goal 自由度）：本次不改 schema，设计上记住"院校/日期将可空"
- **D5**（AI 护栏 + 能力白名单）：阶段 2 加技能前必须立
- **✅ D6 种子**：`CARD_REGISTRY` 已加 `sizes: CardSize[]` 元数据（full/half/quarter，首项默认档，commit 51b771b）；布局数据保持有序数组形状（`workspaceCards: string[]`，可升级 `{id, size, pos}`），默认布局收敛已随页面拥挤治理（a2d8eac）落地；MCP/CLI 薄适配层与桌面壳**阶段 0 不做**，技能生态启动时补

## 关联

- 产品设计原则见 [PROJECT_STATUS.md](./PROJECT_STATUS.md) 与产品讨论记忆（驾驶模式 / AI 角色宪章 / 心路成长表达规范）
- 技能系统 V1 实现细节见 PROJECT_STATUS「第 11/12 轮」与 [docs/](./)
- **掌握度设计见 [completion-model.md](./completion-model.md)**（阶段 0 的 0.1/0.2：五档 + 科目感知 + 对话校准 + 保守软门控）
